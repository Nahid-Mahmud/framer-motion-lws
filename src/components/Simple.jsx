import { motion } from "framer-motion";

const Simple = () => {
    return (
        <motion.div 
      animate= {{
        
      }}
      className="bg-red-500 border w-32 h-32 rounded-full"></motion.div>
    );
};

export default Simple;